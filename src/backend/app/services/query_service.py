# app/services/query_service.py

def process_query_result(text: str, result) -> str:
    # Here, process the result as needed
    # Example: formatting the result into a readable string or JSON response
    response_text = f"Processed result based on the query: {text}\nResult: {result}"
    return response_text
